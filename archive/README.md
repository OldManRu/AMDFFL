# Migration Archive

Historical source material used to rebuild AMD FFL lives here.

- `wordpress-export/` contains the legacy WordPress WXR export and extraction artifacts.
- `source-data/` is reserved for other historical source files such as spreadsheets or RTSports exports.

These files are evidence/source material and should not become runtime dependencies of the public site.
